<Window x:Class="SA.Application.View.WindowMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{DynamicResource CommonFont}"
        ResizeMode="CanResizeWithGrip"
        SizeToContent="WidthAndHeight">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Semantic.UI;component/Resources/Semantic.UI.Common.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/Semantic.UI;component/Resources/Semantic.UI.MenuItem.xaml"/>
                <ResourceDictionary>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="True" />
                        <Setter Property="Padding" Value="{StaticResource LeftRightBottomMargin}" />
                    </Style>
                    <Style x:Key="headerTextBlock" TargetType="TextBlock">
                        <Setter Property="Background" Value="Navy" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="Padding" Value="{StaticResource Padding}" />
                    </Style>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Menu x:Name="menu" HorizontalAlignment="Stretch" Style="{StaticResource Menu}">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel HorizontalAlignment="Stretch"></DockPanel>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="_View">
            </MenuItem>
            <MenuItem Header="_Files">
            </MenuItem>
            <MenuItem Header="_Help" HorizontalAlignment="Right">
            </MenuItem>
        </Menu>
        <Border x:Name="borderMain" Grid.Row="1">
            <StackPanel>
                    <TextBlock Style="{StaticResource headerTextBlock}">XAML-based Localization with Sattellite Assemblies</TextBlock>
                <TreeView x:Name="treeView" Padding="{StaticResource RightMargin}">
                    <TreeViewItem Header="Options" IsSelected="True">
                        <TreeViewItem Header="Layout"></TreeViewItem>
                        <TreeViewItem Header="Display">
                            <TreeViewItem Header="Icons"/>
                            <TreeViewItem Header="Fonts"/>
                            <TreeViewItem Header="Color"/>
                            <TreeViewItem Header="Tabstops"/>
                            <TreeViewItem Header="Folder Tabs"/>
                            <TreeViewItem Header="Custom Columns"/>
                            <TreeViewItem Header="View Mode">
                                <TreeViewItem Header="Auto Switch Mode"/>
                            </TreeViewItem>
                        </TreeViewItem>
                    </TreeViewItem>
                </TreeView>
            </StackPanel>
        </Border>
        <Border Grid.Row="2">
            <StatusBar Style="{StaticResource StatusBar}" HorizontalAlignment="Stretch">
                <StatusBarItem>
                    <TextBlock x:Name="textBoxStatusBarCopyright" Margin="{StaticResource LeftMargin}"/>
                </StatusBarItem>
                <StatusBarItem  HorizontalAlignment="Right">
                    <TextBlock x:Name="textBoxScreenshotStatusBarTool" Margin="{StaticResource ButtonStackRightMargin}"/>
                </StatusBarItem>
            </StatusBar>
        </Border>
    </Grid>
</Window>
